import { ArticleEtoilePipe } from './list-article/article-etoile.pipe';

describe('ArticleEtoilePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleEtoilePipe();
    expect(pipe).toBeTruthy();
  });
});
